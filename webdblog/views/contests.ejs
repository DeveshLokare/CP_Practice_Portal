<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contests</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background: linear-gradient(to right, #848afb, #b3b3c4);
        }

        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown ul {
            display: none;
            position: absolute;
            background-color: #38bdf8;
            min-width: 160px;
            box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
            z-index: 1;
            padding: 0;
            margin: 0;
            list-style: none;
        }

        .dropdown:hover ul {
            display: block;
        }

        .dropdown ul li {
            color: white;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
        }

        .dropdown ul li:hover {
            background-color: #005f73;
        }
    </style>
</head>

<body>
  
    <nav class="flex items-center h-16 w-full text-center text-lg bg-slate-800 text-zinc-50">
        <div class="grid grid-cols-10 w-full p-5">
            <a class="flex text-xl justify-start font-bold" href="/home">CP Portal</a>
            <a class="flex justify-start" href="/problems">Problems</a>
            <a class="flex justify-start" href="/contests">Contest</a>
            <a class="flex justify-start" href="/resources.html">Resources</a>
            <% if(locals.username){ %>
                <a class="col-start-10 flex justify-end" href="/profile"> <%= username %></a>
                <% }
                else{%>
                    <a class="col-start-10 flex justify-end" href="/"> SignUp/login</a><%
                } %>
    
        </div>
    </nav>

    <div class="flex justify-center">
        <div class="flex items-center border rounded-xl text-center text-lg bg-blue-300 text-zinc-50 mt-2 p-4">
            <div class="w-full items-center">
                <div class="justify-self-center cursor-pointer dropdown">Platform ▼
                    <ul class="absolute bg-sky-600 w-40 hidden rounded-lg">
                        <li>Codeforces</li>
                        <li>CodeChef</li>
                        <li>LeetCode</li>
                        <li>AtCoder</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>


    <div class="p-10 pt-7 pb-2 font-bold">
        <div class="grid grid-cols-10 w-full p-5 align-top border rounded-t-xl bg-pink-800">
            <div class="flex justify-self-center col-span-2">Start/End time</div>
            <div class="flex justify-self-center">Duration</div>
            <div class="flex justify-self-center">Time left</div>
            <div class="flex justify-center col-span-3">Event</div>
            <div class="flex justify-center col-span-2">Register</div>
            <div class="flex justify-start">Add to calender</div>
        </div>
    </div>
    <% contests.forEach(contests=> {%>

    <div class="p-10 py-2">
            <div class="grid grid-cols-10 w-full p-4 align-top border rounded-xl bg-pink-200">
                <div class="flex justify-self-center col-span-2" href=""><%=  new Date(contests.startTimeSeconds*1000).toLocaleString() %></div>
                <div class="flex justify-self-center" href=""><%=Math.floor(contests.durationSeconds /3600)%> Hours</div>
                <div class="flex justify-self-center" href="">
                    <% if(contests.relativeTimeSeconds >=0){%>
                        completed  <% }
                         else if (contests.relativeTimeSeconds >=-86400 && contests.relativeTimeSeconds <0){
                            %> <%=-Math.floor(contests.relativeTimeSeconds/3600)-1%> Hours <%= (-Math.floor(contests.relativeTimeSeconds%3600/60))-1 %>Minutes<%
                        }
                        else if (contests.relativeTimeSeconds >=-172800 && contests.relativeTimeSeconds <=-86400){
                            %> <%=Math.floor(contests.relativeTimeSeconds /-86400)%> day <%=- Math.floor((contests.relativeTimeSeconds%86400)/3600)-1 %>Hours<%
                        }
                        else{
                            %> <%= Math.floor(contests.relativeTimeSeconds /-86400)%> days <%= -Math.floor((contests.relativeTimeSeconds%86400)/3600)-1 %>Hours<%
                        } %>
                </div>
                <a class="flex justify-center col-span-3" href="https://codeforces.com/contest/<%=contests.id%>" target="_blank">
                    <%= contests.name %>
                </a>
                <div class="flex justify-center col-span-2">
                    <% if(contests.relativeTimeSeconds <=0){ %>
                 <a class="bg-sky-500 hover:bg-sky-600 rounded-lg text-white col-span-2 py-1 px-2" href="//codeforces.com/contestRegistration/<%=contests.id%>" target="_blank"> Registration </a>
                 <% }
                else{%>
                    <a class="bg-sky-500 hover:bg-sky-600 rounded-lg text-white col-span-2 py-1 px-2" href="https://codeforces.com/contest/<%=contests.id%>/standings" target="_blank">Standings</a><%
                } %>
                </div>
                
                <div class="flex justify-center">
    <% if (contests.relativeTimeSeconds <= 0) { %>
        <button class="bg-sky-500 hover:bg-sky-600 rounded-lg text-white py-1 px-2">
            <a href="https://www.google.com/calendar/render?action=TEMPLATE&text=<%= encodeURIComponent(contests.name) %>&dates=<%= new Date(contests.startTimeSeconds * 1000 + (5 * 3600 + 30 * 60) * 1000).toISOString().replace(/-|:|\.\d+/g, "") %>/<%= new Date(contests.startTimeSeconds * 1000 + contests.durationSeconds * 1000 + (5 * 3600 + 30 * 60) * 1000).toISOString().replace(/-|:|\.\d+/g, "") %>&details=<%= encodeURIComponent(contests.name) %>&location=Codeforces" target="_blank">
                Add to Calendar
            </a>
        </button>
    <% } %>
</div>

            </div>
    </div>
    <% }) %>

    <div class="flex justify-center mt-5">
        <div class="flex items-center">
            <% if (currentPage > 1) { %>
            <a href="/contests?page=<%= currentPage - 1 %>"
                class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-l">Previous</a>
            <% } %>

            <% for (let i = Math.max(1, currentPage - 1); i <= Math.min(currentPage + 1, totalPages); i++) { %>
            <a href="/contests?page=<%= i %>"
                class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 <%= i === currentPage ? 'bg-blue-700' : '' %>">
                <%= i %>
            </a>
            <% } %>

            <% if (currentPage < totalPages) { %>
            <a href="/contests?page=<%= currentPage + 1 %>"
                class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-r">Next</a>
            <% } %>
        </div>
    </div>
</body>
</html>